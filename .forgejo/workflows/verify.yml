name: verify

on:
  push:
    branches: [main]
  pull_request:

jobs:
  verify:
    runs-on: docker-node-runner
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-go@v5
        with:
          go-version: "1.26.x"
      - run: ./scripts/verify.sh
